--etapa 1--
promp:analiza #file:App.jsx e importa el array de #file:libros.js, necesito que se muestre solamente la información de los libros y elimines todo lo referente a la pagina por defecto de react.

resultado: cumplió con las instrucciones de maerna eficiente

utilidad: Muy util.

--etapa 2--
promp:utiliza #file:LibroCard.jsx para que el componente reciba de #file:libros.js por props el título del libro, editorial, anio, estado, rsumen, autores. Muestra la informaciónd e manera ordenada y aplica estilos diferenciado según el estado: verde para "disponible", amarillo para "prestado" y gris pára "reservado". Es sumamente importante que definas los tipos de las props con PropTypes y entrega valores por defecto para evitar errores cuando falte un dato.

renderiza el textp siempre como contenido jsx normal. No uses dangerouslySetInnerHTML. Por ultimo, nod ejes credenciales, claves ni datos sensibles escritos en el codigo.

resultado: el objetivo fue logrado, cumple bien las indicaciones.

utilidad: Util


--etapa 3--
promp:Necesito que #file:ListaLibros.jsx reciba el array de libros por props. Este array debe recorrer con .map() y renderizar un libroCard por cada libro. También se debes asignar la prop key usando el id de cada elemento (No uses el indice del array) por ultimo, en la #file:App.jsx , renderiza #file:ListaLibros.jsx pasándole los datos importados.

resultado:el resultado cumplió con lo estipulado en el prompt

utilidad:fue util


--etapa 4--
promp:vamos a aplicar un estado y filtro seguro. Necesito que crees botones o un selector para filtrar por el estado: "todos", "disponible", "prestado", reservado", desed #file:FiltroEstado.jsx. En #file:App.jsx usa "useState" para guardar el filtro seleccionado y un campo de búsqueda por tiltulo. Aplica ambos filtros sobre el array antes de pasarlo a #file:ListaLibros.jsx y ysa renderizado condicional para mostrar el mensaje "no hay libros que coincidan" cuando el resultado quede vacío,

el campo de busqueda debe ser un componente controlado (su valor vive en el estado de la react)
por ultimo valida y normaliza el texto de busqueda antes de usarlo para no confiar ciegamente en la entrada de usuario.

resultado: el resultado cumplió, el filtro de busqueda funcioan como corresponde.

utilidad: muy util.


--etapa 5--
promp:Destaquemos novedades. Resalta visualmente los elementos con esNovedad: true (ejemplo: una etiqueta "nuevo" o un borde distinto), además agrega un contador que muestre cuántos libros son novedad.

resultado: la novedad de "nuevo" cumple, adicionalmente se genera un contador donde se ven cuantos libros son nuevos.

utilidad:muy util.

